# Trading Bot Dependencies - Version Compatibility Fix
# This file fixes the protobuf/tensorflow version conflict

# Core ML/AI Dependencies with compatible versions
# tensorflow >= 2.16.0 supports protobuf 4.x and 5.x properly
tensorflow>=2.16.0,<3.0.0
# Protobuf 5.x series for compatibility with modern TensorFlow
protobuf>=5.26.0,<6.0.0

# Core Trading Dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
ta-lib
plotly>=5.15.0

# Exchange APIs
python-binance>=1.0.19
ccxt>=4.0.0

# Configuration & Environment  
python-dotenv>=1.0.0
pydantic>=2.0.0
typer>=0.9.0
rich>=13.0.0

# Async & WebSocket Support
aiohttp>=3.8.0
websockets>=11.0.0
asyncio-mqtt>=0.13.0

# Logging & Monitoring
loguru>=0.7.0

# Optional: Advanced ML (if needed)
# torch>=2.0.0
# xgboost>=1.7.0
# lightgbm>=4.0.0

# Data Storage & Caching
redis>=4.5.0
# sqlite3 is built-in with Python - no separate installation needed

# Development & Testing
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# NOTE: This fixes the error:
# "Protobuf gencode version 5.28.3 is exactly one major version older than the runtime version 6.31.1"
# By ensuring protobuf stays in 5.x range which is compatible with TensorFlow 2.16+